@model WebDzivniekuPatversme.Services.Other.PaginatedList<WebDzivniekuPatversme.Models.ViewModels.SheltersViewModel>

@{
    ViewData["Title"] = "Patversmes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
    <h1 class="display-4">Patversmes!</h1>
    <h2 class="display-3">*Filtri patversmes izvēlei*</h2>
    <p>Saraksts ar Patversmēm</p>
</div>

<p>
    <a asp-action="Create">+ Pievienot patversmi</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Meklē pēc nosaukuma: <input type="text" name="filter" value="@ViewData["Filter"]" />
            <input type="submit" value="Meklēt" class="btn btn-default" /> |
            <a asp-action="Index">Atpakaļ uz pilnu listi</a>
        </p>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NameSortParm"]" asp-route-filter="@ViewData["Filter"]">Vārds</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["CapacitySortParm"]" asp-route-filter="@ViewData["Filter"]">Dzīvnieku kapacitāte</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["AddressSortParm"]" asp-route-filter="@ViewData["Filter"]">Adrese</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["PhoneNumberSortParm"]" asp-route-filter="@ViewData["Filter"]">Telefona numurs</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["DateAddedSortParm"]" asp-route-filter="@ViewData["Filter"]">Izveidošanas datums</a>
            </th>
            <th>
                ImagePath
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayTextFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayTextFor(modelItem => item.AnimalCapacity)
                </td>
                <td>
                    @Html.DisplayTextFor(modelItem => item.Address)
                </td>
                <td>
                    @Html.DisplayTextFor(modelItem => item.PhoneNumber)
                </td>
                <td>
                    @Html.DisplayTextFor(modelItem => item.DateAdded)
                </td>
                <td>
                    @Html.DisplayTextFor(modelItem => item.ImagePath)
                </td>
                <td>
                    @if (ViewContext.HttpContext.User.IsInRole("administrator") || ViewContext.HttpContext.User.IsInRole("worker"))
                    {
                        @Html.ActionLink("Labot", "Edit", new { id = item.AnimalShelterID })
                        @Html.Raw(" | ")
                        @Html.ActionLink("Dzēst", "Delete", new { id = item.AnimalShelterID })
                        @Html.Raw(" | ")
                    }

                    @Html.ActionLink("Par", "Details", new { id = item.AnimalShelterID })
                </td>
            </tr>
        }
    </tbody>
</table>

@{
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}

<a asp-action="Index"
   asp-route-sortOrder="@ViewData["CurrentSort"]"
   asp-route-pageNumber="@(Model.PageIndex - 1)"
   asp-route-filter="@ViewData["Filter"]"
   class="btn btn-default @prevDisabled">
    Previous
</a>
<a asp-action="Index"
   asp-route-sortOrder="@ViewData["CurrentSort"]"
   asp-route-pageNumber="@(Model.PageIndex + 1)"
   asp-route-filter="@ViewData["Filter"]"
   class="btn btn-default @nextDisabled">
    Next
</a>